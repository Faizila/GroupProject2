<section id="exercises" class="exercises">
      <div class="container">

        <div class="section-title">
          <h1>Stretching Exercises</h1>
    
          {{#each exercises as |exercise|}}
            {{> stretch-details exercise}}
            {{/each}}  

        </div>

      </div>
    </section>